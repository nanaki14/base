<%
  /* *
   * グローバル変数
   * _meta.ejs 内で読み込み全体で使用する
  */

  // サイトのルート
  // ============================//
  THEME_NAME = 'mytheme'
  siteDir = '/'

  // 各アセットディレクトリへのパス
  // ============================//
  baseDir = {
    css: `${siteDir}assets/wp-content/themes/${THEME_NAME}/css`,
    img: `${siteDir}assets/wp-content/themes/${THEME_NAME}/images`,
    js: `${siteDir}assets/wp-content/themes/${THEME_NAME}/js`,
  }

  // ベースのmeta情報
  // ============================//
  head = {
    title: 'site_title',
    description: 'site_desc',
    favicon16: `${baseDir.img}/favicon-16.png`,
    favicon32: `${baseDir.img}/favicon-32.png`,

    og: {
      url: 'url',
      type: 'type',
      title: 'title',
      image: 'image',
      description: 'description'
    }
  }
%>
